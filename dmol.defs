#if ( defined (__DMOL) || defined(__ACEMD) || defined(__NAMD) )
#define __DECLARE_DMOL_SELECT int :: isele, i__, iend; int, pointer::dmolselect(:)=>NULL();character(LEN=20)::word__

#if ( defined(__ACEMD) || defined(__NAMD) )
#define __GET_IND_FROM_SELECTION(__STRING) minimal_system_getind(__STRING)
#define __NATOM minimal_system_natom
#else
#define __GET_IND_FROM_SELECTION(__STRING) system_getind(__STRING)
#define __NATOM psf_natom()
#endif

#endif
