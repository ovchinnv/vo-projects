#ifdef __ADAPTIVE_KCV
#ifdef __STRUNA

#define __DEP_ADAPTIVE use adaptive_kforce; use cv_common, only: cv, cv_energy=>energy ; use cv_types
#define __VARS_ADAPTIVE int :: cvind = -1, l, adaptive_freq ; character(vartaglen) :: keyword ; int*8 :: ad_iteration ; int, parameter :: default_adaptive_freq=1
#define __ADAPTIVE_CTX 'adaptive_kforce'
#else
#error ADAPTIVE FORCE CONSTANTS REQUIRE STRUNA MODULE
#endif

#else

#define __DEP_ADAPTIVE !
#define __VARS_ADAPTIVE !
#endif
