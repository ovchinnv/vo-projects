12/17 : the idea is to have a single fortran plugin interface, which is callable from different MD codes, and
which calls different (possibly, very different) plugins that live in this tree;
The first version is a more or less brute force concatenation of watershell and string
