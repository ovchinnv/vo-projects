#ifdef __CHARMM
#ifndef __CHARMM_ONLY
#define __CHARMM_ONLY
#endif
#define float real(chm_real)
#define int integer
#define bool logical
#define __WARN(__MSG) call wrndie(0, whoami, __MSG)
#define __DEBUG .false.
#define __CHKDET .true.
#define __DETFAILTRACE .true.
#define __DEPMOD use consta, only: ERRTOL; use chm_kinds
#define __ERRTOL ERRTOL()*50
#define __DEBUG_OUT(__FMT,...) write(666,__FMT) __VA_ARGS__
#else
#define __CHARMM_ONLY !**CHARMM_ONLY**!
#endif
