6/6/24 : piece-wise polynomials
6/10/24 : implemented a check function, which checks for smoothness ; however, the spline in the separation direction is of 2nd order, which generates warnings when
checked ; also, relatively small errors of ~1e-10 get flagged ; not sure whether this function should be enabled by default ; it is too loud a test...
6/10/24 : test case compiles and runs