#ifdef __DMOL
          nullify(iselct)
          isele=__INDX(comlyn, comlen, 'SELE' ,4)
          _MSGBUF=comlyn(isele:comlen)  ! part of string that begins with the selection
          i__=comlen-isele+1
          iend=__INDX(_MSGBUF,i__, 'END',3)   ! location of selection termination
          iend=iend-1+isele                              ! index into comlyn starting from 1
          _MSGBUF=comlyn(isele:iend) ! part of string that begins with the selection and ends before ' END'
          word__=pop_string(_MSGBUF) ! remove first word (which we know is 'SELE*') from _MSGBUF
!     process selection:
          nullify(iselct)
          iselct=>__GET_IND_FROM_SELECTION(_MSGBUF)
!     remove selection string from command line:
          _MSGBUF=comlyn(iend:comlen)  ! command line starting with 'END' (see above) 
          word__=pop_string(_MSGBUF) ! remove 'END*' e.g. 'ENDING' is ok too
          comlyn(isele:isele)=' ';
          comlyn(isele+1:)=_MSGBUF          ! selection has been removed from command
          comlen=len_trim(comlyn)
#endif
